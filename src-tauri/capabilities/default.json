{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the NETSIGHT main window",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
