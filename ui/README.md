# NetSight UI

The UI is a dark-first observability console. The canonical product mark is `../branding/assets/netsight-logo.svg` and must be reused for header, splash, empty states and application icon assets.

Visual tokens live in `ui/styles.css` and mirror `branding/README.md`.
